# svglide.md.mbt

Markdown を H2 (`##`) 区切りで分割し、SVG スライドとしてレンダリングする MoonBit ライブラリです。

## Features

- Front Matter (`slide_theme`) でテーマ指定
- `##` 見出しごとに 1 スライド
- 純 SVG 生成
- speakerdeck 風の HTML deck ページ生成

## Quick Start

```bash
just check
just test
just run
```
